#Here will be any important imports/modules that pokedex.py may need
import discord
from discord.ext import commands
from discord import app_commands
import os
import random

from discord.ext import commands

'''
Pokédex function format:
    if message.content.startswith("!ro POKEMON NAME"):
    
        await message.channel.send("NAME, the TYPING type Pokémon.")
        await message.channel.send("Pokédex entry NO###.")

        num = random.randint(1,3)

        if num == 1:
            await message.channel.send()

        if num == 2:
            await message.channel.send()

        if num == 3:
            await message.channel.send()
'''











